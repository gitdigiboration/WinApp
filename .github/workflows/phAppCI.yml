
name: .NET Desktop CI

on:
  push:
    branches: [ Development ]
  pull_request:
    branches: [ Development ]

jobs:

  build:

    strategy:
      matrix:
        configuration: [Release]

    runs-on: windows-latest  # For a list of available runner types, refer to
                             # https://help.github.com/en/actions/reference/workflow-syntax-for-github-actions#jobsjob_idruns-on
    env:
      Solution_Name: PhApp.sln                         # Replace with your solution name, i.e. MyWpfApp.sln.
     # Test_Project_Path: WinAppUnitTest\WinAppUnitTest.csproj               # Replace with the path to your test project, i.e. MyWpfApp.Tests\MyWpfApp.Tests.csproj.
    #  Wap_Project_Directory: your-wap-project-directory-name    # Replace with the Wap project directory relative to the solution, i.e. MyWpfApp.Package.
     # Wap_Project_Path: your-wap-project-path                   # Replace with the path to your Wap project, i.e. MyWpf.App.Package\MyWpfApp.Package.wapproj.

    steps:
    - name: Checkout
      uses: actions/checkout@v2
      with:
        fetch-depth: 0
    # Install the .NET Core workload
    #- name: Install .NET Core
    #  uses: actions/setup-dotnet@v1
     # with:
    #    dotnet-version: 5.0.x

    # Add  MSBuild to the PATH: https://github.com/microsoft/setup-msbuild
    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v1.0.2
   # - name: Setup Nuget
    #  uses: Nuget/setup-nuget@v1.0.2

    #- name: Restore Nuget Packages
     # uses: nuget restore PhApp.sln
      
    # Execute all unit tests in the solution
  #  - name: Execute unit tests
   # dotnet test ~/projects/test1/test1.csproj
      #run: dotnet test ~\WinAppUnitTest\WinAppUnitTest.csproj
   # - name: Build the Solution
   #   run: msbuild PhApp\PhApp.csproj

    # Restore the application to populate the obj folder with RuntimeIdentifiers
    #- name: Restore the application
    #  run: msbuild $env:Solution_Name /t:Restore /p:Configuration=$env:Configuration
    #  env:
     #   Configuration: ${{ matrix.configuration }}

  #  # Decode the base 64 encoded pfx and save the Signing_Certificate
   # - name: Decode the pfx
   #   run: |
   #     $pfx_cert_byte = [System.Convert]::FromBase64String("${{ secrets.Base64_Encoded_Pfx }}")
       # $certificatePath = Join-Path -Path $env:Wap_Project_Directory -ChildPath GitHubActionsWorkflow.pfx
       # [IO.File]::WriteAllBytes("$certificatePath", $pfx_cert_byte)

    # Create the app package by building and packaging the Windows Application Packaging project
   # - name: Create the app package
     # run: msbuild $env:Wap_Project_Path /p:Configuration=$env:Configuration /p:UapAppxPackageBuildMode=$env:Appx_Package_Build_Mode /p:AppxBundle=$env:Appx_Bundle /p:PackageCertificateKeyFile=GitHubActionsWorkflow.pfx /p:PackageCertificatePassword=${{ secrets.Pfx_Key }}
     # env:
    #    Appx_Bundle: Always
      #  Appx_Bundle_Platforms: x86|x64
      #  Appx_Package_Build_Mode: StoreUpload
     #   Configuration: ${{ matrix.configuration }}

    # Remove the pfx
   # - name: Remove the pfx
     # run: Remove-Item -path $env:Wap_Project_Directory\$env:Signing_Certificate
     
     #Upload the MSIX package: https://github.com/marketplace/actions/upload-a-build-artifact
   # - name: Upload build artifacts
   # - uses: actions/upload-artifact@v2
     #    with:
     # name: MSIX Package
     # path: ${{ env.Wap_Project_Directory }}\AppPackages
    - uses: jfrog/setup-jfrog-cli@v2
      #env:
       # JF_ARTIFACTORY_1: "eyJ2ZXIiOiIyIiwidHlwIjoiSldUIiwiYWxnIjoiUlMyNTYiLCJraWQiOiJfaXNIMmNHeWRvRjZIckpRcWdqdTF4ZFJZSFpGSUJtbnA0UkYtWFVHby1ZIn0.eyJzdWIiOiJqZmZlQDAwMFwvdXNlcnNcL2FkbWluIiwic2NwIjoiYXBwbGllZC1wZXJtaXNzaW9uc1wvYWRtaW4gYXBpOioiLCJhdWQiOiIqQCoiLCJpc3MiOiJqZmZlQDAwMCIsImlhdCI6MTYzNzc3MzczMCwianRpIjoiZGNjNjNhNTQtZTE1NS00MTk0LWJiY2ItYTExM2IxMTdlNjRiIn0.d6Scne69lrf8Zbjj04ha7dZHpwWkBkZB4r8bvvSRP4tZLe66c1XobR4GVmlO04CVhJASz5QrHXKNAEhz3RMGBLmn5e00TQ-irYEuZBlYbSyflwuOlygrJyHKFf4MZXlXz8tWKzBWUvK51g_XxfnHYHoIkl-Z4UjWxY3SJYlCiCkH9vo6r3-wJ8frN84WWj-sXJ7IDbH1jnGn4U5cyJHCk7m03zDxMRjnbKBUDs5MG4rr77-dOpQlLh8LLztths3HaZC4O-6uy1PrDV1yrpxT_BEh_lvUsgATRHWR9WfYHaQyywaPqo5msSs_LWiDfMBTmM5KR7mz7JV1C71Gwemw8g"
    - run: |
           # Ping the server jfrog
            #jfrog rt ping --url=http://localhost:8082/artifactory
            jfrog rt c --url=http://localhost:8082/artifactory --user=admin --password=password
